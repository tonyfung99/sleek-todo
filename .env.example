NODE_ENV=development
PORT=3000
DATABASE_URL=postgres://sleek:sleek@postgres:5432/sleektodo
REDIS_URL=redis://redis:6379
JWT_SECRET=dev-only-change-me-0123456789
CORS_ORIGIN=http://localhost:5173

# Host ports published by `docker compose` (override if a port is already in
# use, e.g. another app on 3000). Defaults shown.
API_PORT=3000
WEB_PORT=5173
POSTGRES_PORT=5432
REDIS_PORT=6379
